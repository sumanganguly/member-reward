definitions:
  errors:
    type: "object"
    properties:
      errors:
        type: "array"
        items:
          $ref: "#/definitions/errorModel"

  errorModel:
    type: "object"
    properties:
      message:
        type: string
        description: nessage to the consumer reagrding the error

